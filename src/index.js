import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App 
    // 이 부분이 우리가 component를 사용하고자 할 때, component의 형태임. 즉 이것이 component를 사용하는 방법이다.
    />   
  </React.StrictMode>,
  document.getElementById('root')
);

